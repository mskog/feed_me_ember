dependencies:
  override:
    - npm install
    - bower install
deployment:
  master:
    branch: master
    commands:
      - ember build --environment="production"
      - rsync -avz -e "ssh" dist/ deployer@144.76.27.197:/var/www/feed_me_ember/
